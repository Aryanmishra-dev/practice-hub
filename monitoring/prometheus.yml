# Prometheus configuration for Quiz Forge

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "quiz-forge-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8000"]
        labels:
          service: "quiz-forge-api"
